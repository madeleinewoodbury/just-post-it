<!-- HEADER -->
<header id="main-header" class="py-2 bg-warning text-white">
  <div class="container">
    <div class="row">
      <div class="col-md-6">
        <h1><i class="fas fa-folder"></i> Categories</h1>
      </div>
    </div>
  </div>
</header>

<!-- SEARCH -->
<section id="search" class="py-2 mb-4 bg-light">
  <div class="container">
    <div class="row">
      <div class="col-md-6 ml-auto">
        <form action="/post/search/categories" method="post">
          <div class="input-group">
            <input type="text" class="form-control" placeholder="Search Categories..." name="search" />
            <div class="input-group-append">
              <button type="submit" class="btn btn-warning">Search</button>
            </div>
          </div>
        </form>
      </div>
    </div>
  </div>
</section>

<!-- CATEGORIES -->
<section id="categories">
  <div class="container">
    <div class="row">
      <div class="col-md-9">
        <div class="card">
          <div class="card-header">
            <h4>Latest Categories</h4>
          </div>
          <table class="table table-striped">
            <thead class="thead-dark">
              <tr>
                <th>#</th>
                <th>Categories</th>
                <th>Title</th>
                <th>Posted By</th>
                <th>Date Posted</th>
              </tr>
            </thead>
            <tbody>
              {{#each posts}}
              <tr>
                <td>{{postIndex @index}}</td>
                <td><a href="/post/show/{{id}}">{{category}}</a></td>
                <td>{{title}}</td>
                <td>{{user.name}}</td>
                <td>{{formatDate date 'MM/DD/YY'}}</td>
              </tr>
              {{/each}}
            </tbody>
          </table>
        </div>
      </div>
      <div class="col-md-3">
        {{!-- <div class="card text-center bg-primary text-white mb-3">
          <div class="card-body">
            <h3>Posts</h3>
            <h4 class="display-4"><i class="fas fa-pencil-alt"></i> {{numberOfPosts posts}}</h4>
          </div>
        </div> --}}
        <div class="card text-center bg-warning text-white mb-3">
          <div class="card-body">
            <h3>Categories</h3>
            <h4 class="display-4"><i class="fas fa-folder"></i> {{numberOfCategories posts}}</h4>
          </div>
        </div>
        {{!-- <div class="card text-center bg-danger text-white mb-3">
          <div class="card-body">
            <h3>Users</h3>
            <h4 class="display-4"><i class="fas fa-users"></i> {{numberOfUsers posts}}</h4>
          </div>
        </div> --}}
      </div>
    </div>
  </div>
</section>